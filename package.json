{
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "packageManager": "^npm@10.9.2",
  "devDependencies": {
    "turbo": "^2.4.2"
  }
}
